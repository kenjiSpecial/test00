"use strict";
const THREE = require('three');

export default class TraiLine extends THREE.Mesh {
    constructor() {
        var verticeNum = 100;

        var geo = new THREE.Geometry()

        super(geo, mat)
    }
}